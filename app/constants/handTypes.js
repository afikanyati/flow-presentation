/**
 * Used by the Viewport component as a list of permissible enum
 * values that represent the permissible choices for one's strong hand
 */

export default {
    RIGHT: "right",
    LEFT: "left"
};
